# PRD —— 迷你聚合热搜（mini-hot）

> 版本：v1.2 ｜ 初稿：2026-09-18（Day 4）｜ 最近更新：2026-09-18（视觉方向调整）｜ 作者：Panky（与 AI 协作）
> 前置文档：`research.md`（竞品调研与 MVP 方案定稿，Day 3）
> 文档性质：**产品需求文档**。只描述「做什么、给谁用、怎么算做完」，不描述技术实现；具体怎么抓数据、怎么部署，写在后续的 `TECH_DESIGN.md` 里。
> **当前版本重点变化**：视觉主张由「纸感日报」调整为「**星空为底、内容为主角**」，并新增深浅模式要求与二期白噪音功能。变更全过程见文末「变更记录」。

---

## 一、产品概述

### 1.1 一句话定位

> **一个手机友好的单页网站：打开就能看到几个主流平台「今天大家都在看什么」，点标题跳回原平台。**

### 1.2 它解决什么问题

- **省去逐个平台翻榜的时间**：想看今天有什么热点，不必分别打开微博、百度、B站、GitHub，一个页面看完。
- **给开发者留一个入口**：主流聚合产品普遍面向大众，几乎不收录开发者关心的 GitHub Trending。mini-hot 把「技术趋势」和「大众热点」放在同一个页面里，形成差异化。
- **提供一个自己能掌控的信息入口**：不被平台的推荐流推着走，而是主动「看完就走」。

### 1.3 它是什么（定位坐标）

| 维度 | mini-hot 的选择 |
|---|---|
| 内容规模 | 4 个来源，每源 Top 20~50 条 |
| 更新频率 | 小时级（非实时） |
| 使用场景 | 每天主动打开几次，每次 1~3 分钟 |
| 产品气质 | 极简、克制、安静、有夜色感 |
| 项目性质 | 学习作品 + 真实自用工具，**不追求商业化** |

### 1.4 它不解决什么

明确不承担的期待，避免范围蔓延：

- **不做**「热点追踪工具」：不提供趋势分析、热度曲线、事件时间线。
- **不做**「内容消费平台」：只跳转原文，不搬运正文、不做站内阅读。
- **不做**「个性化推荐」：所有人看到的榜单完全一致（与微博/知乎的全站统一榜单同理）。
- **不做**「商业聚合站」：不追求来源数量，不与今日热榜、NewsNow 比功能广度。

---

## 二、目标用户

### 2.1 主用户（第一优先级）

**「每天想知道外面发生了什么、但不想被 App 拖着走的年轻人」。**

| 画像要素 | 描述 |
|---|---|
| 身份 | 学生 / 职场新人 / 对信息敏感的普通网民 |
| 使用设备 | 手机为主（预估 70%），PC 为辅 |
| 使用时机 | 早晨通勤、午休、睡前；每次 1~3 分钟 |
| 核心诉求 | 「今天有什么值得知道的？」—— 快速扫一眼，够了就走 |
| 典型动作 | 打开页面 → 切 Tab 扫标题 → 挑一两条点进去看原文 → 关掉 |
| 讨厌什么 | 弹窗、登录墙、无限滚动、自动播放、内容推荐流、**花哨到影响阅读的装饰** |

### 2.2 次用户（第二优先级）

**开发者 / 技术爱好者**（即本项目作者本人这类用户）：

- 关心 GitHub Trending，但不想每天专门开 GitHub。
- 在同一个页面里从「技术趋势」扫到「大众热点」，是他们独有的爽点。

### 2.3 非目标用户（明确不服务）

- 需要分钟级实时监控热点的**公关 / 营销 / 媒体从业者**——他们是商业产品的用户，mini-hot 的小时级更新满足不了。
- 需要历史数据回溯的**研究者 / 数据分析师**——本期无存档。
- 需要跨设备同步收藏的**重度用户**——本期收藏仅存本机。

### 2.4 用户的一句话期待

> 「我就想知道今天有什么热闹，**别让我注册、别让我等、别给我推东西**。」

这条期待是全部设计决策的裁判：任何让用户多等一秒、多填一个框、多看一眼广告的功能，都不做。

**同一原则也适用于视觉**：星空是氛围，不是噪音。用户在读标题时不应注意到背景——这也是为什么背景必须是**静态、低对比**的（详见 §3.1.1 与原则 1）。

---

## 三、设计原则（Day 3「审美表现力」的具体落地）

### 3.1 这条原则的来历

Day 3 立项反思时确认了一条判断：**mini-hot 的「独一无二」不能靠平台数量取胜**（那是 NewsNow、今日热榜用工程量和维护成本堆出来的护城河，个人项目卷不过），**只能靠两件事**：

1. **审美表现力**——同样一堆数据，我做得更好看、更好读；
2. **真实自用**——我自己每天都打开它，所以每个细节都会被我亲手打磨过。

因此，审美不是「最后加个皮肤」，而是**并列于功能的第一等需求**，必须在 PRD 阶段就写成可判断的原则，而不是留到开发时即兴发挥。

### 3.1.1 视觉主张（2026-09-18 二次拍板）

Day 4 起草本 PRD 时，视觉方向最初写成「纸感日报」。当天下午根据新的想法做了**一次方向调整**，本 PRD 已按新方向定稿：

> **视觉主张：星空为底，内容为主角。**
> 界面是一块安静的夜空，热榜标题是夜空中最清晰的那些字。

调整记录（诚实留存，便于日后回看为什么改）：

| 项目 | 原方案（Day 4 初稿） | 定稿方案（2026-09-18 二次拍板） |
|---|---|---|
| 视觉主张 | 白纸感、纸感日报 | **星空氛围，内容仍是主角** |
| 原「原则 1」 | 白纸感（单一背景色，禁渐变/阴影） | 改写为 **「星空为底，安静不抢戏」** |
| 深浅模式 | 未提及（默认浅色） | **默认跟随系统 + 顶部手动切换按钮** |
| 宇航员元素 | 无 | 作为**极小点缀**（空状态/页脚微插画），不成主角 |
| 白噪音 | 无 | 列为**二期功能 F12**，本期不做 |

**保持不变的关键取舍**：原则 2（一屏一个源）、原则 3（标题是主角）、原则 4（来源可辨识）、原则 5（安静地失败）**一字未改**。理由：换视觉不能换掉「好读」这条底线——星空是背景，不是内容。原「白纸感」原则的精神（装饰不抢戏）被完整继承为新原则 1。

### 3.2 五条可执行的设计原则

以下每条都能被「问一句话」判定是否合格，避免变成空口号。

#### 原则 1：星空为底，安静不抢戏 —— 装饰永远在内容之下

> 本条为原「白纸感」的改写版本。精神不变（装饰不得干扰阅读），表达从「纸」换成「夜」。

- **含义**：页面主体永远是「文字」，背景的星空只是氛围。星空必须**低对比、低饱和、静态可忽略**；用户在读标题时不应注意到它。
- **判定句**：「把背景星空换成纯深色，所有标题的可读性会变差吗？」——答「不会变差」才合格。
- **落地要求**：
  - 星空以**低对比的星点/微光**呈现，不使用大面积星云、不强发光；
  - 星点不得落在标题文字的低对比区域，正文区域背后应保持干净；
  - 不使用投影、不阻塞文字对比度；装饰层永远位于内容层之下；
  - 深色模式与浅色模式**两套背景都要满足本条**（浅色模式的星空 = 极淡的纸纹理或近乎纯浅色，不是深色星空翻白）。
- **反面清单（明确禁止）**：流星雨动画、闪烁星点、旋转星球、全屏发光、自动滚动的星空背景。

#### 原则 2：一屏一个源 —— 不制造滚动疲劳

- **含义**：用户选定一个来源后，**前 10 条必须在一屏之内看完**，无需滚动即可掌握「今天大概是什么风向」。
- **判定句**：「手机竖屏不滚动，能看到至少 10 条？」——答「是」才合格。
- **落地要求**：单条高度克制（序号 + 标题 + 必要时的热度值），一屏 10~12 条。**背景装饰不得增加任何垂直空间**（星空只能是底层，不能占位）。

#### 原则 3：标题是主角 —— 文字排版必须被认真对待

- **含义**：产品唯一的「内容」就是标题。字号、行高、字重、长标题截断方式，是这个项目最值得花时间的细节。
- **判定句**：「一条 40 个汉字的长标题，是否仍然整齐、不被腰斩得难看？」——答「是」才合格。
- **落地要求**：正文标题字号不低于 15px；标题最多 2 行，超出以省略号收尾；序号与标题对齐基线一致；**深浅两套模式下标题都必须达到可读对比度**。

#### 原则 4：来源可辨识 —— 4 个源要有一眼可分的身份

- **含义**：用户在 Tab 之间切换时，应能立刻感知「我换到另一个平台了」，而不是看到一份换汤不换药的列表。
- **判定句**：「截掉 Tab 栏，只看列表，能看出这是哪个源吗？」——答「是」才合格。
- **落地要求**：每个来源有固定的标识色（同一色系内做区分，不喧宾夺主）与来源名；不同源的「特色数据」要显示出来（微博的热度值、B站的播放量、GitHub 的 star 数——这是各源真正的性格）。**来源色必须在深色和浅色两套模式下都保持可辨识**（不能出现深色下好看、浅色下发虚的情况）。

#### 原则 5：安静地失败 —— 异常状态不打断阅读

- **含义**：数据源挂掉是常态（详见第八节）。此时页面必须优雅降级，**绝不能用弹窗、红字警告、全屏报错中断用户**。
- **判定句**：「某个源挂了，用户还能正常看其他三个源吗？」——答「是」才合格。
- **落地要求**：单源失败只在对应 Tab 内以一句浅色说明文字表示；全站不出现 alert 类阻断式提示。**深浅模式切换失败（如系统主题读取异常）也必须安静降级**为默认外观，不报错。

### 3.3 视觉基调（方向性描述，正式视觉稿在第 4 周打磨）

- **气质关键词**：夜、安静、广阔、克制；像深夜独自抬头看星空，而不是进入一个太空主题乐园。
- **配色方向**：
  - 深色模式（主场）：深空底（深蓝黑/近黑）+ 浅色正文 + 低饱和强调色；星点用极低对比的白/淡蓝。
  - 浅色模式：近白底 + 深灰正文 + 同一套强调色；**不放大面积深色星空**，仅保留极淡的纸感纹理以呼应主题。
  - 两套模式共用同一套强调色与来源色，保证「切了模式还认得出这是 mini-hot」。
- **宇航员元素的使用规则**：**只作极小点缀，绝不成主角**。
  - 允许出现的位置：空状态插画（如收藏列表为空时）、页脚角落的微插画。
  - 禁止：首屏大面积插画、随滚动移动的插画、作为 Logo 占位、出现在榜单列表区域内。
  - 判定句：「如果删掉宇航员，功能是否完全不受影响？」——答「是」才合格（即它只能是点缀）。
- **禁止项**：霓虹色、高饱和大色块、闪烁动效、自动轮播、流星雨/旋转星球等持续动画、卡通吉祥物风格的拟人宇航员。
- **说明**：本节只给方向，具体色值、字体、间距在第 4 周视觉打磨阶段产出，届时以「设计原则 1~5」为验收依据。

---

## 四、MVP 功能（本期必做）

> 书写约定：每个功能用 **「用户动作 → 可观察结果」** 描述。凡不能用肉眼或点击验证的，不算需求。
> MVP 范围 = 首页热搜列表 + 详情查看 + 一个收藏/备注动作。**不含登录、复杂推荐、付费**（见第五节）。

### F1. 首页热搜列表

**F1.1 打开即见榜**

- **用户动作**：在浏览器地址栏输入网址并回车（手机/PC 均可）。
- **可观察结果**：页面直接显示榜单内容，无需登录、无需点击任何「进入」按钮、无任何弹窗或遮罩层；首个来源的榜单已经展开。
- **验收要点**：从输入网址到看见榜单标题，无需任何额外交互。

**F1.2 来源切换**

- **用户动作**：点击顶部的来源 Tab（微博 ｜ 百度 ｜ GitHub ｜ B站）。
- **可观察结果**：当前 Tab 有明确的选中样式；下方列表内容立刻整体替换为该来源的榜单；切换过程不出现整页白屏闪烁。
- **验收要点**：4 个 Tab 逐一可切换，每个 Tab 都能看到该源自己的数据。

**F1.3 浏览榜单条目**

- **用户动作**：滚动或静置查看当前来源的列表。
- **可观察结果**：每条包含排名序号、标题、来源标识；同一来源内序号从 1 连续递增，不跳号、不重复；长标题不会被截断成半句话。
- **验收要点**：任取一个来源，条目数与序号连续性能对上（例：微博 Top 50 则应看到 1~50 连续）。

**F1.4 看到榜单的时效信息**

- **用户动作**：看向页面顶部站名附近。
- **可观察结果**：能看到当前来源的数据更新时间（形如「更新于 14:00」），以及榜单类型说明（如「实时热搜」「日榜」）。
- **验收要点**：时间来自数据本身，不是页面加载时间；刷新页面时间不变（因为数据没变）。

### F2. 详情查看（跳转原平台）

**F2.1 点标题跳原文**

- **用户动作**：点击任意一条的标题。
- **可观察结果**：在新标签页打开该条目在原平台的真实页面（微博话题页 / 百度热搜结果页 / B站视频页 / GitHub 仓库页），能看到对应内容。
- **验收要点**：4 个来源各抽 3 条测试，链接指向的页面内容与列表标题一致，无 404、无跳转到平台首页。

**F2.2 详情信息的「预览」而非「搬运」**

- **用户动作**：鼠标悬停或长按查看条目附加信息（如来源、热度值）。
- **可观察结果**：能在不离开列表的情况下看清该条目的来源与热度信息；**点击行为永远只有「去原平台」一种结果**。
- **验收要点**：产品内不存在「站内详情页」——本期不做内容搬运，这是合规底线（详见第八节 8.4 与第十节）。

> **为什么详情不做站内页**：正文版权属于原平台/原作者。mini-hot 只做「指路」，不做「转载」。这既是合规要求，也是极简定位的必然结果。

### F3. 收藏（本期唯一的用户主动变更动作）

> **存储方式（2026-09-18 拍板）**：**存浏览器本地（localStorage）**。
> 理由：不需要账号、不需要数据库、不需要服务器，与已定技术路线 A（纯静态站）完全一致，零基础可实现。
> 代价：数据只存在当前浏览器，换设备或清除浏览器数据后会丢失；**云端同步列为后续功能**（见 F5）。

**F3.1 收藏一条**

- **用户动作**：在列表某条上点击「收藏」入口（图标或文字按钮）。
- **可观察结果**：该条目的收藏状态立即变为「已收藏」（图标实心/文字变为「已收藏」）；**用户不离开当前列表**；不需要登录，不出现任何弹窗索要账号。
- **验收要点**：收藏后立刻刷新页面，该条目仍显示为已收藏。

**F3.2 取消收藏**

- **用户动作**：再次点击已收藏条目的收藏入口。
- **可观察结果**：状态立即回到未收藏；刷新页面后仍是未收藏状态。
- **验收要点**：反复点击 3 次，状态与最后点击一致，不出现错乱。

**F3.3 查看我的收藏**

- **用户动作**：点击「我的收藏」入口（位于来源 Tab 区域或页面顶部）。
- **可观察结果**：切到一个收藏列表，只显示已收藏的条目；**每条必须显示它来自哪个源**（因为收藏是跨源混合的）；收藏为空时显示一句友好的空状态文案（如「还没有收藏，去榜单里挑几条吧」），并给出回到榜单的入口。
- **验收要点**：收藏 3 条（分别来自不同源）后进入收藏列表，3 条都在且来源标识正确；全部取消后显示空状态文案而非空白页。

### F4. 页脚声明（合规必需，工作量极小）

- **用户动作**：滚动到页面底部。
- **可观察结果**：能看到数据来源说明与免责声明，大意包含：数据来自各平台公开榜单、版权归原作者/原平台所有、本站在跳转后不复制内容、仅作个人学习用途。
- **验收要点**：4 个来源名称全部被列出；声明文字可见、可读，不需要点击展开。

### F5. 关于「备注」（范围说明，已定稿）

需求原文是「至少一个收藏**或**备注动作」。**决策（2026-09-18 拍板）：本期只做收藏，不做备注。**

- **理由**：收藏是零成本动作（一次点击），备注需要输入框、需要处理保存/编辑/删除三态、还要考虑本机存储容量与 UI 空间。在 MVP 阶段增加备注会稀释「极简」定位，且与「一屏看完」的排版原则冲突。
- **备注的去向**：列入**二期**（见第五节 F7），届时与收藏列表一起做完整。
- **结论**：本期「用户主动变更数据的动作」只有收藏一项，即 F3。备注在 MVP 期间不出现于任何页面。

---

## 五、后续功能（本期明确不做）

> 分为两档：**二期（近，全平台聚合愿景的第一步延伸）** 与 **远期（不设时间表）**。
> 本节同时是防范围蔓延的「缓冲区」——开发中有新想法，先记到这里，不在 MVP 期间动手。

### 5.1 二期功能

#### F6. 数据源扩展 —— **「全平台聚合」愿景落地**

- **这是什么**：这是 Day 3 立项反思里定下的**二期愿景**。本期 4 个源（微博 / 百度 / GitHub / B站）只是**第一级台阶**，跑通的是「多源聚合」这套骨架；二期把这套骨架复制到更多平台。
- **候选来源**（按 research.md 的实测难度排序）：
  1. **知乎热榜**——本期二选一时落选，官方接口有签名门槛，二期攻。
  2. **澎湃 24h 最热榜**——严肃新闻向，能中和娱乐化内容。
  3. **抖音热点 / 今日头条**——覆盖短视频人群。
  4. **财新 / 36氪**等垂类源——面向特定兴趣。
- **用户动作**：在来源 Tab 区域看到更多来源，或进入一个「来源管理」入口。
- **可观察结果**：可以勾选自己想看的来源；勾选后 Tab 栏只显示已勾选来源；未勾选来源不出现在页面上。
- **实现前提（诚实说明）**：来源越多，抓取失败率越高、维护成本越高。这一期必须有「单源失效不拖垮全站」的机制（见第八节），否则加源等于给自己挖坑。

#### F7. 收藏增强（含备注，补齐 F5 的缺口）

> **备注的正式归属地**：本期明确不做备注（见 §四 F5），备注功能从这一项开始做。

- **收藏备注**：给已收藏条目写一句话备忘（如「这个选题可以借鉴」）。
  - **用户动作**：在收藏列表中点击某条的「加备注」。
  - **可观察结果**：出现一个输入区，输入并保存后，该备注显示在这条下方；可再次编辑/清空。
- **收藏分组或标签**：把收藏归入自定义分类（如「工作」「好玩」「待看」）。
- **云端同步**：解决 F3「换设备就丢」的问题。
  - **用户动作**：在另一台设备上打开网站并登录。
  - **可观察结果**：能看到此前在别处收藏的条目。
  - **实现前提**：这一步才真正需要**账号系统**（见 F9）与云端存储，属于工作量的数量级跃升，必须二期再评估。

#### F8. 历史与趋势

- **历史榜单存档**：保留每天/每小时的榜单快照，可回看某天大家在关注什么。
- **热度趋势**：某条话题是否有「在涨」的趋势，用简单的箭头或迷你曲线表示。
  - **用户动作**：点击某条右侧的趋势标识。
  - **可观察结果**：看到该话题近几个时间点的热度变化。
  - **实现前提**：需要长期存储与图表能力，与「免服务器、免数据库」的现状冲突，成本最高，排在二期靠后。

#### F9. 白噪音（「平静内心」功能，2026-09-18 新增）

- **这是什么**：为「星空氛围」补上听觉维度——看榜时可选播放一段循环白噪音（雨声 / 海浪 / 夜晚虫鸣等），让这个页面成为一个可以安静待一会儿的地方。
- **为什么列入二期不做**（诚实说明）：
  1. 它不是视觉效果，而是**一个完整的小播放器**——需要音频资源、播放/暂停、音量控制、状态记忆，工作量独立于本期任何功能；
  2. 需要准备**可自托管的音频文件**（涉及授权与仓库体积），而本期「零依赖」的定位不宜引入媒体资源；
  3. 手机浏览器**禁止自动播放音频**，必须用户主动点击才能发声——它天然是一个按钮，不能「打开就响」，因此也无法靠它营造「打开即安静」的效果。
- **用户动作**：点击页面上的「白噪音」按钮，并从若干音源中选择一个。
- **可观察结果**：选中的噪音开始循环播放；按钮变为「播放中」状态；再点一次暂停。音量可在页面内调整，无需跳转其他应用。
- **可观察结果（持久化）**：手动选择后，下次打开页面时按钮记住上次选的音源，但**不会自动播放**（浏览器限制，也避免突然出声）。
- **可观察结果（不影响阅读）**：播放中切换来源 Tab、切换主题、收藏条目，音频都**不中断**。
- **必须遵守的纪律**：
  - 绝不自动播放、绝不自动切换音源；
  - 默认音量为低音量，不出现「突然大声」；
  - 关闭页面即停止播放，不留后台声音；
  - 按钮的视觉存在感必须低于榜单内容（不得因为新增按钮而挤压缩小列表，对应设计原则 2）。
- **实现前提**：需要先解决音频素材的来源与授权问题。**如果找不到可靠的免费音源，这一项应继续推迟，不要用低质量音源凑数。**

### 5.2 远期功能（列出来是为了明确「现在不做」）

#### F10. 账号系统（登录）

- **范围**：注册 / 登录 / 找回密码 / 跨设备数据同步。
- **为什么本期不做**（research.md §6.4 已定）：涉及数据库、身份验证、安全与隐私合规，工作量在 MVP 基础上翻数倍；且对「1~3 分钟看榜」的使用场景而言，登录墙本身就是体验倒退——用户在 2.4 节的原话是「别让我注册」。
- **触发条件（什么时候才做）**：当 F7 的云端同步被真实需要时（例如我确实开始在手机和电脑上都用、且收藏丢过）。

#### F11. 个性化推荐

- **范围**：基于浏览/收藏行为，为不同用户呈现不同的榜单排序或内容。
- **为什么本期不做**：与产品定位直接冲突。research.md 记录过，知乎热榜的优点之一正是「全站用户看到同一份榜单」——它是「公共议题的温度计」，千人千面会摧毁这个属性。
- **触发条件**：只有在产品定位从「公共热榜」转向「个人信息流」时才考虑——目前**不打算转**。

#### F12. 付费功能 / 变现

- **范围**：会员、订阅、广告、打赏、导流分成等任何形式的收入。
- **为什么本期不做**：①数据版权不属于本项目，拿他人平台的数据变现存在明确法律风险；②项目性质是学习作品与个人自用工具，无商业目标（research.md §6.4 明确列出「不碰」）。
- **结论**：**本期与二期均不做**。若未来开放，必须先解决数据授权问题，而不是先做收费功能。

#### F13. 其他被排除项（沿用 research.md §6.4）

站内搜索、关键词过滤、AI 摘要、分钟级实时刷新、App / 小程序 / PWA、多语言。理由见 research.md，此处不重复。

> **范围变更记录**：「暗色模式」原在排除列表中，已于 2026-09-18 移入**本期做**（见 §六 6.2.1 深浅模式）；「白噪音」为同期新增的二期功能（F9）。其余排除项不变。

---

## 六、页面与交互要求

### 6.1 页面结构（只有 1 个页面）

```
┌──────────────────────────────────┐
│  站名 mini-hot   [☾]   [我的收藏] │   ← 顶部：站名 + 主题切换 + 收藏入口
├──────────────────────────────────┤
│  微博 ｜ 百度 ｜ GitHub ｜ B站     │   ← 来源 Tab 栏
├──────────────────────────────────┤
│  更新于 14:00 · 实时热搜          │   ← 当前源的时效说明
├──────────────────────────────────┤
│  1   标题文字……            [收藏] │
│  2   标题文字……            [收藏] │   ← 榜单列表
│  3   标题文字……            [收藏] │
│  …                                │
├──────────────────────────────────┤
│  数据来源与免责说明               │   ← 页脚
└──────────────────────────────────┘
        ↑ 星空作为整页底层氛围，位于所有内容之下
```

> **结构说明**：星空是页面的**底层背景层**，不占据任何垂直空间——上面这张结构图里的每个区块都浮在星空之上，不会因装饰而变矮或移位。

### 6.2 各区块交互要求

| 区块 | 要求 |
|---|---|
| 站名 | 静态文字，点击不做跳转或仅回到本页顶部；不使用 Logo 图片（减少加载负担）；**不使用宇航员作为站名图形** |
| 主题切换按钮 | 位于顶部右侧、与收藏入口同层级；**点击立即切换深浅模式，无需刷新页面、不打断当前阅读、不滚动到顶部**；按钮本身有明确的当前状态提示（如显示「☾ 深色」/「☀ 浅色」） |
| 我的收藏入口 | 与来源 Tab 同一视觉层级，点击后切换到收藏视图；再点当前来源 Tab 可回到榜单 |
| 来源 Tab 栏 | 4 个 Tab 等宽排列，手机上不折行、不横向滚动；当前 Tab 有明确选中态；**切换主题后选中态仍清晰** |
| 时效说明 | 显示「更新于 HH:MM」；不同源可显示各自的时间窗口说明（如 GitHub 显示「今日 / 本周」） |
| 榜单条目 | 序号 + 标题 + （热度值，若有）+ 收藏按钮；标题可点击跳转，收藏按钮独立可点击且不触发跳转 |
| 页脚 | 固定在内容末尾（非悬浮固定），随页面滚动；**宇航员插画（若有）在此区域的角落，尺寸极小** |

### 6.2.1 深浅模式（新增，2026-09-18 拍板）

**模式定义与默认值**

- **默认策略**：打开页面时**自动跟随操作系统的深浅设置**（手机/电脑处于深色模式则页面为深色，反之浅色）。
- **手动覆盖**：顶部主题切换按钮可手动切换，**手动选择后在本机记住该选择**（下次打开不再被系统设置覆盖）。
- **模式范围**：仅两套——深色（主场，星空底）与浅色。

**切换行为**

- **用户动作**：点击顶部主题切换按钮。
- **可观察结果**：整个页面颜色立即切换（背景、文字、Tab、收藏态同步变化）；**页面不发生重新加载、不白屏、不回到顶部、当前选中的来源 Tab 与滚动位置保持不变**。
- **可观察结果（持久化）**：切换后刷新页面或关闭再打开，仍是上次手动选择的模式。
- **可观察结果（首次访问）**：在没有手动选择过的前提下，用系统深色模式打开应为深色，切到系统浅色再打开应为浅色。

**两套模式的共同要求**

- **对比度**：标题正文在两种模式下都必须清晰可读（禁止深色下灰字压深底、浅色下浅字压白底）。
- **来源色一致**：4 个来源的标识色在两套模式下保持同一色相，仅调整明度为适配背景（保证「切了模式还认得出这是哪个源」）。
- **装饰对称**：两套模式都要满足设计原则 1；**浅色模式不是深色模式简单反色**，而是单独一套近白底 + 极淡纹理的方案。

### 6.3 移动端要求（第一优先）

- **手机竖屏为第一设计目标**，PC 端为「合理即可」。
- 一屏内可见至少 10 条（对应设计原则 2），**星空装饰不得挤占条目的垂直空间**。
- 触控目标（Tab、主题切换、收藏按钮）足够大，相邻可点区域之间不误触；顶部有 3 个可点元素，需注意间距避免误触。
- 不做横向滚动；长标题自动换行或省略，不撑破布局。
- 不使用任何依赖鼠标悬停才能看到的信息——悬停态属于 PC 增强，不是必需的。
- 主题切换按钮在手机上必须单手可达（位于顶部区域内）。

### 6.4 PC 端要求（够用即可）

- 页面内容居中，最大宽度做限制，避免长标题在大屏上拉成一行横条难以阅读。
- 允许在一屏内容纳更多条目（可自然多于手机）。
- 不做多栏布局，保持与手机一致的阅读顺序——**避免维护两套布局**。
- 星空背景在大屏上**不放大星点密度**（星点尺寸与密度以手机为准，大屏两侧留空），避免装饰感变重。

### 6.5 交互细则

- **点击反馈**：Tab 切换、主题切换与收藏点击需有即时视觉反馈（颜色/状态变化），让用户确认操作已生效。
- **不打断**：全站不使用弹窗、不使用浏览器 alert 提示、不使用全屏加载遮罩。
- **无自动行为**：不自动刷新、不自动轮播、不自动播放（**含不自动发声**）、不自动跳转。
- **返回友好**：从原平台返回时，页面停留在原来的来源、滚动位置与**上次选择的主题模式**（保持浏览连续性）。
- **动效克制**：仅允许轻微的状态过渡（如颜色渐变、主题切换的柔和过渡），禁止位移/缩放类动效、禁止持续循环的背景动画（对应设计原则 1）。
- **性能克制**：星空装饰不得影响滚动流畅度；在低性能设备上应能自动降级为纯色背景（此降级对用户表现为「背景看起来很干净」，而非报错）。

---

## 七、数据字段

> 描述「页面上呈现的每一条热榜记录由哪些信息构成」。
> 每个源都可能有自己的特色字段，用「必要 / 可选 / 源特有」三档标注，抓取实现留到 TECH_DESIGN。

### 7.1 一条热榜记录的最小字段（所有源共用）

| 字段 | 必要性 | 说明 | 从哪来 |
|---|---|---|---|
| `source` | 必要 | 来源标识（微博/百度/GitHub/B站） | 抓取时按源写入 |
| `rank` | 必要 | 排名序号，正整数 | 原榜单的排名 |
| `title` | 必要 | 条目标题文字 | 原榜单标题 |
| `url` | 必要 | 跳转原平台的链接 | 原榜单的链接地址 |
| `updatedAt` | 必要 | 该源的抓取更新时间（精确到分钟） | 抓取执行时刻 |

**说明**：这 5 个字段对应 research.md §4 得出的「最小可用集 = 排名 + 标题 + 链接 + 更新时间」，是任何一个源都必须提供的。缺任一字段的条目**不应出现在页面上**（宁可少一条，不可出现点不动的死链）。

### 7.2 热度与标签字段

| 字段 | 必要性 | 说明 |
|---|---|---|
| `heat` | 可选 | 热度值（数字或已格式化的字符串，如「512.3万」）。各源口径不同，**必须原样显示并标明所属源**，不可跨源换算比较 |
| `heatLabel` | 可选 | 热度值的口径名，如「热搜指数」「star 数」「播放量」，避免用户误以为 4 个源的热度可以横向比 |
| `tags` | 可选 | 状态标签（如微博的「热/新/沸」、百度的「热/新」）。**原样呈现，不自行发明标签** |

### 7.3 各来源的特色字段

| 来源 | 特色字段 | 页面是否显示 |
|---|---|---|
| 微博热搜 | 热度值、状态标签（新/热/沸等）、置顶标记 | 显示热度值与标签；置顶标记可显示 |
| 百度热搜 | 内容摘要、配图、「热/新」标签 | 显示标签；摘要与配图**可选**（考虑排版整洁，倾向不显示配图） |
| GitHub Trending | 仓库名、简介、主要语言、总 star、窗口内新增 star、fork 数 | 显示语言与 star 数（这是这个源的性格），简介可选 |
| B站排行榜 | UP 主、播放量、弹幕数、综合评分 | 显示 UP 主与播放量 |

> **一条重要约束**：`heat` 的单位与量级在 4 个源之间**不可比**（微博是互动指数、GitHub 是 star 数、B站是播放量）。因此页面上绝不能做「跨源热度排序」，也不得出现「全站热度总榜」这类暗示可比的呈现。

### 7.4 榜单级（非条目级）字段

| 字段 | 必要性 | 说明 |
|---|---|---|
| `listTitle` | 必要 | 榜单名称，如「微博热搜榜」「GitHub Trending · 今日」 |
| `listType` | 必要 | 时间窗口类型：实时 / 24h / 日 / 周 |
| `listUpdatedAt` | 必要 | 该源最后一次成功更新的时间 |
| `listStatus` | 必要 | 该源的取数状态：正常 / 失败（驱动第八节的异常展示） |
| `itemCount` | 可选 | 本榜条目总数，用于校验序号连续性 |

### 7.5 收藏记录的字段

| 字段 | 必要性 | 说明 |
|---|---|---|
| `title` | 必要 | 收藏时的标题快照 |
| `url` | 必要 | 收藏时的链接 |
| `source` | 必要 | 来源（收藏是跨源混合的，**必须显示**） |
| `savedAt` | 必要 | 收藏时刻，用于收藏列表排序（新的在上） |
| `rank` / `heat` | 可选 | 收藏时的排名/热度快照，可帮助回忆当时为什么收藏 |

> **存储位置（已拍板）**：仅存浏览器本地，不上传、不跨设备。页脚或收藏视图内需有一句说明，让用户知道「收藏保存在本机」。

---

## 八、异常状态

> 对应设计原则 5「安静地失败」。核心要求：**任何异常都不阻断用户看其他源**。

### 8.1 数据源类异常

| 编号 | 场景 | 用户看到什么 | 用户能做什么 |
|---|---|---|---|
| E1 | **单个来源抓取失败**（接口变了、被限流） | 切到该 Tab 时显示一句浅色说明，如「这个来源暂时取不到数据，稍后重试」，并标注「上次成功更新：昨天 20:00」 | 切换到其他 3 个源，一切正常 |
| E2 | **全部来源抓取失败** | 页面仍可打开，各 Tab 内均为一句说明；页面顶部有一条不刺眼的提示（如「数据暂时没有更新」） | 看到提示，知道是数据问题而非自己网络问题；页面不白屏 |
| E3 | **某源数据为空**（抓到了但 0 条） | 显示「该来源当前没有内容」+ 建议切其他源；**不显示空白区域** | 切到其他源 |
| E4 | **条目缺字段**（拿到标题但没有链接） | 该条**不显示**，其余条目正常 | 正常浏览；不得出现点了没反应的条目 |
| E5 | **数据过期**（超过预期更新周期未更新，如超 6 小时） | 时效说明处把时间显示为警示态（如「更新于 08:00 ⚠」或改用次要提示色） | 用户自行判断数据新鲜度；页面不弹窗 |

### 8.2 网络与加载类异常

| 编号 | 场景 | 用户看到什么 |
|---|---|---|
| E6 | 用户自身网络不通 | 页面框架正常渲染，数据区域显示「加载失败，请检查网络后刷新」+ 一个手动重试入口 |
| E7 | 数据加载中 | 显示与真实列表结构相近的骨架/占位，**不使用全屏转圈遮罩**；加载超过合理时间自动切到 E6 提示 |
| E8 | 加载极慢 | 先渲染已到达的部分，不等待全部数据到位（先给用户能看的，再补齐） |

### 8.3 用户操作类异常

| 编号 | 场景 | 用户看到什么 |
|---|---|---|
| E9 | 收藏时本机存储不可用（浏览器隐私模式等） | 一次性的轻提示「当前浏览器无法保存收藏」，收藏功能降级但不影响看榜；**不弹窗、不报错堆栈** |
| E10 | 收藏数量过多 | 达到上限时提示已达上限，建议清理旧收藏；不静默丢弃用户数据 |
| E11 | 收藏的链接已失效 | 收藏列表中该条仍可见（保留历史），点击时按原平台的失效表现处理；不在本产品内做失效检测（成本过高） |

### 8.4 内容与合规类异常

| 编号 | 场景 | 处理方式 |
|---|---|---|
| E12 | 某源出现敏感或不适宜内容 | 本项目不搬运、不审核、不二次编辑——页面只展示原平台给出的标题并跳转。这是「只指路不转载」带来的天然边界 |
| E13 | 原平台要求停止抓取 | 立即下线该源；因数据源被设计为可配置、可替换（research.md §7.1），单源下线不需要重构 |
| E14 | 重大事件的信息准确性 | 遵循 research.md §3.7 的结论：以权威媒体报道为准绳。产品内不做事实核查，但在页脚声明中提示「以原平台与权威媒体信息为准」 |

### 8.5 异常处理的统一纪律

1. **不阻断**：任何异常都不得阻止用户浏览其他来源。
2. **说实话**：明确告诉用户「是数据没更新」还是「是你网络的问题」，不含糊成「出错了」。
3. **给退路**：每个异常状态都提供一个可点击的下一步（切换来源 / 重试 / 刷新）。
4. **不变形**：异常提示的文字与排版，必须与正常列表同一套视觉语言，不出现刺眼的红色告警块。

---

## 九、验收标准

> 逐条可勾选。**全部为「可以亲眼看到」的项**，不接受口头结论。
> MVP 的完成定义：第一至四组全部通过 + 第五组无阻断项。

### 第一组：能打开、能看

| 编号 | 验收标准 | 状态 |
|---|---|---|
| A1 | 在手机上打开网址，无需登录、无任何弹窗，首页直接显示榜单内容 | ☐ |
| A2 | 页面顶部能看到站名与「更新于 HH:MM」的时间信息 | ☐ |
| A3 | 4 个来源 Tab 都能点击切换，每次切换列表内容确实变为该源的数据 | ☐ |
| A4 | 每个来源的条目排名序号从 1 开始连续，不跳号、不重复 | ☐ |
| A5 | 手机竖屏不滚动，至少能看到 10 条（对应设计原则 2） | ☐ |

### 第二组：能点、能收藏

| 编号 | 验收标准 | 状态 |
|---|---|---|
| B1 | 4 个来源各抽 3 条点击标题，都能在新标签打开原平台对应页面，内容与标题一致 | ☐ |
| B2 | 点击收藏按钮，该条立即变为「已收藏」且不离开当前列表 | ☐ |
| B3 | 收藏后刷新页面，收藏状态仍在（验证本地存储生效） | ☐ |
| B4 | 再次点击可取消收藏，刷新后仍为未收藏 | ☐ |
| B5 | 「我的收藏」视图能列出跨来源的收藏条目，且每条显示来源 | ☐ |
| B6 | 无收藏时，收藏视图显示友好空状态文案而非空白页 | ☐ |

### 第三组：审美与交互（对应第三节 5 条原则）

| 编号 | 验收标准 | 状态 |
|---|---|---|
| C1 | 把背景星空换成纯色，所有标题的可读性不会变差（原则 1：星空为底不抢戏） | ☐ |
| C2 | 一条 40 字长标题不撑破布局，截断处整齐（原则 3：标题是主角） | ☐ |
| C3 | 只看列表内容、遮住 Tab 栏，能分辨出这是哪个源（原则 4：来源可辨识） | ☐ |
| C4 | 全站无弹窗、无 alert、无自动刷新、无自动跳转 | ☐ |
| C5 | 触控目标在真机上不误触（Tab、主题切换、收藏按钮三个都要测） | ☐ |
| C6 | PC 上内容居中且有最大宽度限制，长标题不被拉成一行横条 | ☐ |
| C7 | 星空为纯静态，无闪烁、无流星、无旋转等持续动画 | ☐ |
| C8 | 页面滚动流畅，背景装饰未造成明显卡顿（低性能设备可降级为纯色且无报错） | ☐ |
| C9 | 宇航员元素（若有）位置符合规则：仅在空状态/页脚，不在榜单区域内 | ☐ |

### 第三组之二：深浅模式（新增，2026-09-18）

| 编号 | 验收标准 | 状态 |
|---|---|---|
| M1 | 首次访问时，页面外观跟随系统深浅设置（系统深色则为深色） | ☐ |
| M2 | 点击主题切换按钮，页面颜色立即整体切换，**不刷新、不白屏、不回顶部** | ☐ |
| M3 | 切换主题后，当前选中的来源 Tab 与滚动位置保持不变 | ☐ |
| M4 | 手动切换后刷新或重开页面，仍保持上次选择的模式（验证本机记忆生效） | ☐ |
| M5 | 深色模式下标题清晰可读，浅色模式下同样清晰可读（无低对比文字） | ☐ |
| M6 | 4 个来源标识色在两套模式下都能分辨，且色相一致 | ☐ |
| M7 | 浅色模式是独立的近白方案，不是深色模式的简单反色（不能出现大面积深色星空） | ☐ |
| M8 | 系统主题信息读取异常时，页面安静降级为默认外观，不报错、不弹窗 | ☐ |
| M9 | 在系统深色模式下把页面切成浅色，刷新后仍保持浅色（手动选择优先于系统） | ☐ |

### 第四组：异常状态（对应第八节）

| 编号 | 验收标准 | 状态 |
|---|---|---|
| D1 | 手动让某一个源取不到数据，该 Tab 显示说明文字，其他 3 个源正常可看（E1） | ☐ |
| D2 | 断网状态下打开页面，显示网络提示与重试入口，不白屏（E6） | ☐ |
| D3 | 数据未到位时显示占位而非全屏遮罩，且不会长时间卡在加载态（E7） | ☐ |
| D4 | 隐私模式下尝试收藏，出现轻提示且不影响看榜（E9） | ☐ |
| D5 | 页脚能看到数据来源与免责声明（F4） | ☐ |

### 第五组：范围与合规（不通过则不算完成）

| 编号 | 验收标准 | 状态 |
|---|---|---|
| E-1 | 全站无任何登录 / 注册入口 | ☐ |
| E-2 | 全站无任何付费、会员、广告位 | ☐ |
| E-3 | 全站无站内详情页、无正文搬运，点击行为只有「跳原平台」 | ☐ |
| E-4 | 无跨源热度换算或「全站热度总榜」这类暗示可比的呈现（见 §7.3 约束） | ☐ |
| E-5 | 页脚声明包含 4 个来源名称与「仅作个人学习用途」 | ☐ |
| E-6 | 页面不采集任何个人信息，除收藏与主题偏好（均仅存本机）外无任何数据写入 | ☐ |
| E-7 | **全站无任何音频自动播放、无任何后台发声**（白噪音属二期，本期不应出现） | ☐ |

### 关于「完成」的诚实说明

- 第一、二、四组需要**真实数据接入后**才能验证——第 2 周用假数据搭页面时，这些项标注为「待验证」，不算通过。
- 第三组（含深浅模式组）中的 C1、C2、C3、M5、M7 是主观判断项，由你自己肉眼判定，AI 不代替你打分。
- 第五组为**否决项**：任何一条不通过，MVP 不予发布。

---

## 十、范围边界（硬清单）

> 摘自 research.md §6.4，作为开发期间的防蔓延清单。**有新产品想法，先把想法写进第五节的「后续功能」，不要当场实现。**

### 本期做（仅此清单内）

- 4 个来源热榜展示：微博热搜、百度热搜、GitHub Trending、哔哩哔哩排行榜
- 来源 Tab 切换 + 手机端自适应
- 标题点击跳转原平台
- 显示数据抓取时间
- 小时级定时更新
- 部署到 GitHub Pages
- **收藏（仅存本机）** ← 本 PRD 在 MVP 中新增的主动数据变更动作
- **深浅模式（跟随系统 + 手动切换）** ← 2026-09-18 二次拍板新增，详见 §6.2.1
- **星空氛围视觉（氛围版：星空为底、内容为主角）** ← 同日新增，详见 §3.1.1 与 §3.2 原则 1
- 页脚来源声明与免责说明

### 本期不做

| 排除项 | 原因 |
|---|---|
| 账号系统（注册/登录） | 与「别让我注册」的用户期待直接冲突，工作量大 → 列为 F10 |
| 收藏备注 / 分组 / 云端同步 | 本期只做收藏本身，增强项列入 F7 |
| 自定义订阅来源 | 已有商业产品做得很好 → 列为 F6 的一部分 |
| 历史存档、热度趋势图 | 需要长期存储，28 天内容易烂尾 → 列为 F8 |
| 站内搜索、关键词过滤 | 单页几十条直接扫一眼就够 |
| AI 摘要、个性化推荐 | 与「公共热榜」定位冲突 → 列为 F11 |
| 分钟级实时刷新 | 小时级够用，高频抓取增加被封风险 |
| App / 小程序 / PWA | 单页响应式已覆盖手机 |
| 10+ 来源全聚合 | 本期 4 源是「第一级台阶」，扩源属二期愿景 → 列为 F6 |
| **白噪音 / 音频播放** | 需要音频资源与播放器，属独立功能 → 列为二期 F9（2026-09-18 拍板） |
| **宇航员成为视觉主角** | 与「标题是主角」「一屏 10 条」冲突；仅允许极小点缀 → 见 §3.3 |
| 广告、会员等任何变现 | 数据版权不属于本项目 → 列为 F12 |
| 多语言、国际化 | 纯装饰性，放二期以后 |
| ~~暗色模式~~ | **已移出本表**：2026-09-18 拍板列入本期做，见 §6.2.1 |

---

## 附：与 research.md 的对应关系

| 本 PRD 章节 | 来源 |
|---|---|
| 一、产品概述 | research §6.1 一句话定位 |
| 二、目标用户 | research §5 定位启示（学习作品 + 极简自用） |
| **三、设计原则（含 §3.1.1 视觉主张）** | **research §5 立项反思（Day 3）：审美表现力 + 真实自用**；视觉主张经 2026-09-18 二次拍板调整为「星空为底」 |
| 四、MVP 功能 | research §6.3 本期功能白名单 + 本清单要求（收藏动作） |
| **五、后续功能** | **research §5 立项反思：全平台聚合二期愿景** + research §6.4 排除清单 + 2026-09-18 新增 F9 白噪音 |
| 六、页面与交互 | research §6.2 核心页面 + 2026-09-18 新增 §6.2.1 深浅模式 |
| 七、数据字段 | research §4 热榜六要素 |
| 八、异常状态 | research §7 风险与注意事项 |
| 九、验收标准 | 综合，对齐 research 的定位与合规底线 + 新增 M 组（深浅模式）与 C7~C9 |
| 十、范围边界 | research §6.4 本期不做 + 2026-09-18 范围变更记录 |

---

## 变更记录

| 日期 | 版本 | 变更内容 | 决策人 |
|---|---|---|---|
| 2026-09-18 上午 | v1.0 | PRD 初稿：10 章结构、5 条设计原则（原「白纸感」）、MVP = F1~F4、收藏存本机 | Panky |
| 2026-09-18 15:06 | v1.1 | 确认本期不做备注，F5 定稿为「只做收藏」 | Panky |
| 2026-09-18 15:13 | v1.2 | **视觉方向调整**：原「白纸感」→ 原则 1 改写为「星空为底，安静不抢戏」；新增 §3.1.1 视觉主张、§6.2.1 深浅模式（跟随系统+手动切换）；新增二期 F9 白噪音；后续功能编号 F9~F13 顺延；验收标准新增 M 组与 C7~C9、E-7；范围边界同步 | Panky |

> **关于 v1.2 的范围说明**：本次变更**扩大了本期范围**（深浅模式从「不做」移入「做」，新增星空视觉要求）。这是经用户明确拍板后执行的，不是擅自扩大。星空视觉的落地工作量主要在**第 4 周的视觉打磨阶段**，不影响第 1~3 周的数据与页面结构工作——第 2 周用假数据搭页面时即可先行验证原则 2（一屏 10 条）是否仍成立。
